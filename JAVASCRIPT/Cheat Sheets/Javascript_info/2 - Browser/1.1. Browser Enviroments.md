## A - Entorno de Ejecución (Host Environment): El "Hogar" de tu JavaScript 🔴

#### 1. **Introducción:**

Es, sencillamente, el lugar donde tu código JavaScript "vive" y se ejecuta, como un navegador web, un servidor o hasta una cafetera inteligente.

#### 2. **Ejemplo:**

Imagina que JavaScript es un chef. El "entorno de ejecución" es la cocina donde trabaja.

- **Cocina de Restaurante (Servidor con Node.js):** Tiene hornos industriales, cuchillos profesionales y acceso a un gran almacén. Puede hacer cosas muy potentes "detrás de escena".
- **Cocina de Casa (Navegador Web):** Tiene un microondas, una nevera y utensilios para servir la comida en un plato bonito. Está diseñada para interactuar con el "comensal" (tú, el usuario).

El chef (JavaScript) es el mismo, pero las herramientas que puede usar cambian totalmente según la cocina (el entorno) en la que se encuentre.

#### 3. **Desarrollo**:

Cada entorno le ofrece a JavaScript un conjunto de herramientas especiales. El navegador le da herramientas para controlar páginas web. Un servidor (como Node.js) le da herramientas para gestionar archivos y redes. Por eso, un código que funciona en el navegador podría no funcionar en un servidor, y viceversa. ¡No es que el código esté mal, es que está en la "cocina" equivocada!

🔴 **Fundamental**: Entender esto es la base de todo. Te evita la confusión de "¿por qué este código funciona aquí pero no allá?". Te ayuda a saber qué "herramientas" tienes disponibles en cada momento.

---

## B - El Objeto `window`: El Gran Jefe del Navegador 🔴

#### 1. **Introducción:**

Es el objeto principal y más importante en el navegador; representa la ventana del navegador en sí y contiene **absolutamente todo** lo demás.

#### 2. **Ejemplo:**

```javascript
// Rol 1: Es el objeto global, el "dueño" de todo.
function decirHola() {
  alert("¡Hola!");
}
// Llamar a la función directamente...
decirHola();
// ...es EXACTAMENTE lo mismo que llamarla a través de window.
window.decirHola();

// Rol 2: Representa la ventana y te permite controlarla.
alert("La altura de tu ventana es: " + window.innerHeight + " píxeles");
```

**Explicación del ejemplo:**
Fíjate en la magia. En la primera parte, cuando creas una función como `decirHola`, JavaScript secretamente la convierte en un método del objeto `window`. ¡Por eso `window.decirHola()` funciona! En la segunda parte, le estamos pidiendo directamente a `window` una de sus propiedades: su altura (`innerHeight`). `window` es tanto el contenedor de tu código como la herramienta para manipular el propio navegador.

#### 3. **Desarrollo**:

Piensa en `window` como el tronco de un gran árbol. De él salen todas las demás ramas importantes, como el DOM y el BOM (que veremos a continuación). Todo lo que pasa en el navegador, vive dentro de `window`.

![Diagrama del objeto window y sus hijos](https://javascript.info/article/browser-environment/windowObjects.svg)

🔴 **Fundamental**: Es imposible trabajar con JavaScript en un navegador sin interactuar, directa o indirectamente, con `window`. Es el punto de partida, el objeto global, el jefe de todo.

---

## C - DOM (Document Object Model): El Mapa Interactivo de tu Página Web 🔴

#### 1. **Introducción:**

El DOM es la representación de tu archivo HTML como una estructura de objetos, permitiéndote usar JavaScript para leer y modificar el contenido de la página como si fueran piezas de Lego.

#### 2. **Ejemplo:**

```javascript
// Imagina que tu HTML tiene un <body>.
// Con JavaScript, puedes acceder a él y cambiar su estilo.

// 1. Cambia el color de fondo del cuerpo de la página a un tono suave.
document.body.style.backgroundColor = "lightblue";

// 2. Después de 2 segundos, cambia el texto de un título.
// (Suponiendo que tienes un <h1> con id="titulo" en tu HTML)
setTimeout(() => {
  const miTitulo = document.getElementById("titulo");
  if (miTitulo) {
    // Siempre es bueno comprobar si el elemento existe
    miTitulo.innerText = "¡El DOM lo cambió todo!";
  }
}, 2000);
```

**Explicación del ejemplo:**
`document` es tu puerta de entrada al DOM. A partir de ahí, es como seguir un mapa: `document.body` te lleva al objeto que representa la etiqueta `<body>`. Luego, `.style` te da acceso a sus estilos CSS, y `.backgroundColor` te permite cambiar el color de fondo. ¡Estás modificando la página en tiempo real! No estás editando el archivo HTML original, sino la versión "viva" que el navegador tiene en memoria.

#### 3. **Desarrollo**:

La palabra clave aquí es **`document`**. Este objeto, que vive dentro de `window` (`window.document`), es tu principal herramienta para trabajar con el DOM. A través de él puedes encontrar cualquier elemento, cambiar su contenido, su estilo, crear nuevos elementos o eliminarlos.

🔴 **Fundamental**: Si quieres que tu página web sea dinámica (que reaccione a los clics, que muestre nueva información, que valide formularios...), necesitas dominar el DOM. Es la esencia de la interactividad en la web.

---

## D - BOM (Browser Object Model): El Panel de Control del Navegador 🟡

#### 1. **Introducción:**

El BOM es el conjunto de herramientas que te da el navegador para interactuar con el navegador en sí, pero **no** con el contenido de la página (eso es tarea del DOM).

#### 2. **Ejemplo:**

```javascript
// Usando el objeto 'location' del BOM para trabajar con la URL.
alert("Estás en la página: " + location.href);

// Ahora, le preguntamos al usuario si quiere cambiar de página.
const irAGoogle = confirm("¿Te gustaría ir a Google.com?");

if (irAGoogle) {
  // Si dice "Aceptar", redirigimos el navegador a una nueva URL.
  location.href = "https://google.com";
}
```

**Explicación del ejemplo:**
Observa que aquí no estamos cambiando un color o un texto _dentro_ de la página. Estamos trabajando con características del navegador:

1.  `location.href`: Nos da la URL actual.
2.  `confirm()`: Abre un diálogo del navegador, no un elemento HTML.
3.  `location.href = "..."`: Le da una orden directa al navegador para que cargue una página nueva.

Esto es el BOM en acción: controlar el "marco", no la "pintura".

#### 3. **Desarrollo**:

El BOM incluye objetos muy útiles como:

- `navigator`: Te da información sobre el navegador del usuario y su sistema operativo (`navigator.userAgent`).
- `location`: Te permite leer la URL actual o redirigir a una nueva.
- Funciones como `alert()`, `confirm()` y `prompt()`, que son métodos de comunicación del navegador.

**¡La Diferencia Clave para que no te confundas NUNCA!**

- **DOM**: Se ocupa del **contenido** de la pestaña del navegador (el documento HTML). Piensa en ello como el editor de un documento de Word.
- **BOM**: Se ocupa del **navegador** en sí (la ventana, la URL, el historial). Piensa en ello como los botones y menús del programa Word (Abrir, Guardar, Imprimir).

🟡 **Importante**: Aunque quizás no lo uses tan intensamente como el DOM en el día a día, es esencial para tareas comunes como gestionar URLs, mostrar alertas o detectar el tipo de navegador.

---

## E - CSSOM (CSS Object Model): El Estilo Hecho Objeto 🔵

#### 1. **Introducción:**

Es un "primo" del DOM, pero en lugar de representar el HTML, representa tus reglas de CSS como un conjunto de objetos que puedes manipular con JavaScript.

#### 2. **Ejemplo:**

Este es un caso de uso menos común, pero imagina que quieres cambiar una regla de estilo para **todos** los elementos que usan una clase, no solo uno.

```javascript
// EJEMPLO CONCEPTUAL:
// Supongamos que en tu CSS tienes: .alerta { color: red; }

// Con CSSOM, podrías encontrar esa regla y cambiarla para todo el sitio.
// (El código real es un poco más complejo, pero la idea es esta)
// document.styleSheets[0].cssRules[0].style.color = "purple";

// Después de ejecutar este código, CUALQUIER elemento con la clase 'alerta'
// que se añada a la página, ahora aparecerá en color morado, no rojo.
```

**Explicación del ejemplo:**
La diferencia es sutil pero poderosa. Con el DOM, cambiarías el estilo de _un elemento específico_ (`document.getElementById('miAlerta').style.color = 'purple'`). Con el CSSOM, cambias la _definición de la clase_ `.alerta` en sí misma para toda la página.

#### 3. **Desarrollo**:

En la práctica, rara vez necesitarás manipular el CSSOM directamente. ¿Por qué? Porque es mucho más fácil y común simplemente añadir o quitar clases de CSS a los elementos usando el DOM (`elemento.classList.add('clase-nueva')`). Sin embargo, es bueno saber que el CSSOM existe por si alguna vez te enfrentas a un problema muy específico que requiera modificar reglas de estilo globales de forma dinámica.

🔵 **Específico**: Conócelo, sabe que existe, pero no te obsesiones con él al principio. Para el 99% de las tareas de estilado dinámico, usar el DOM para gestionar clases es el camino a seguir.
